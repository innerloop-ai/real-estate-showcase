// Define the interface for property data
export interface Property {
  id: number;
  title: string;
  price: string;
  address: string;
  bedrooms: number;
  bathrooms: number;
  area: number;
  image: string;
}